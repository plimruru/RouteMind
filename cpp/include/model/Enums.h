#pragma once

enum class Skill {
    ConnectionWorks,
    LocalWorks,
    EmergencyWorks
};

enum class Transport {
    Car,
    Pedestrian,
    Bicycle,
    PublicTransport
};

enum class Priority {
    Normal,
    Urgent
};