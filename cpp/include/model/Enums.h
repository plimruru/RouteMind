enum class Skill {
    LocalWorks,
    ConnectionWorks,
    EmergencyWorks
};

enum class Transport {
    Car,
    Pedestrian,
    Bicycle,
    PublicTransport
};

enum class Priority {
    Normal,
    Urgent
};