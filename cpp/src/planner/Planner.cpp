#include "planner/Planner.h"

Plan Planner::solve(
    const std::vector<Request>& requests,
    const std::vector<Engineer>& engineers
) {
    Plan plan;

    return plan;
}

Plan Planner::replan(
    const std::vector<Request>& requests,
    const std::vector<Engineer>& engineers,
    const Event& event
) {
    Plan plan;

    return plan;
}